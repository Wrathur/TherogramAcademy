<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wrathur.course.mapper.StudentHomeworkMapper">
    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO student_homework
        (student_id, homework_id, review_status, is_deleted, create_time, update_time)
        VALUES
        <foreach collection="list" item="sh" separator=",">
            (#{sh.studentId}, #{sh.homeworkId}, #{sh.reviewStatus}, #{sh.isDeleted}, #{sh.createTime}, #{sh.updateTime})
        </foreach>
    </insert>
</mapper>